<%= form_for(@donator) do |f| %>
  <% if @donator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@donator.errors.count, "error") %> prohibited this donator from being saved:</h2>

      <ul>
      <% @donator.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
